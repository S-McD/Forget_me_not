<!DOCTYPE html>
<html lang="en">
  <head>  
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title>User Dashboard</title>
  </head>
  <body>
    <%- include('partials/signed_in_header') %>
    <h1>User Dashboard</h1>

    <div class="user-card">
      <img src="/images/user.jpg" width="100px">
      <br>
      Hello <%=user.first_name%> <%=user.last_name%>.
      <br>
      You have <%=events.length%> events in your calander.
      <br>
      You have <%=requests.length%> requests to reply to.
      <a href="#" class="events">
        <span>Inbox</span>
        <span class="badge"><%=events.length%></span>
      </a>
    </div>
    
<br>
    <form method = 'GET' action = '/events/new_event'> 
      <input type = 'submit' value = 'Create Event'>
    </form> 
      <% events.forEach( (event) => { %>
      <h1> <%= event.name %> </h1>
      <form method="get" action="/requests/party_invite/<%=event._id%>">
        <button type="submit"> Invite </button>
      </form>
    <% }) %>

    <%- include('partials/footer') %>

  </body>
</html>  
  